interface Card {
    id: string;
    cardNumber: string;
    cvv: string;
    expirationMonth: number;
    expirationYear: number;
    email: string;
    createdAt: string;
    expiredAt: string;
}

export default Card;